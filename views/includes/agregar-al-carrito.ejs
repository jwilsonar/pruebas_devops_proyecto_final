<form action="/carrito" method="post">
    <button class="px-8 py-2 border border-gray-400 rounded-lg hover:bg-gray-400 hover:text-white transition-all" type="submit">Agregar al Carrito</button>
    <input type="hidden" name="idProducto" value="<%= producto._id %>">
</form>